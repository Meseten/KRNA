# Core runtime dependency of the krna package.
numpy>=1.26.0

# Optional extras (also available as pip extras: krna[plots], krna[ml]):
# matplotlib>=3.8.0   # plotting: krna benchmark / krna mo-benchmark
# scikit-learn>=1.3.0 # krna tune / SKROAMLTuner
# joblib>=1.3.0       # parallel CV evaluation in SKROAMLTuner
